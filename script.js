const nav=document.querySelector('.nav');const menu=document.querySelector('.menu-toggle');menu?.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const products=[...document.querySelectorAll('.product')];document.querySelectorAll('.filter').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;products.forEach(p=>p.classList.toggle('hidden',f!=='all'&&p.dataset.category!==f))}));
let cart=JSON.parse(localStorage.getItem('eliteWearCart')||'[]');const count=document.getElementById('cartCount');const updateCount=()=>count.textContent=cart.length;updateCount();
document.querySelectorAll('.order').forEach(btn=>btn.addEventListener('click',()=>{const name=btn.dataset.product;cart.push(name);localStorage.setItem('eliteWearCart',JSON.stringify(cart));updateCount();const text=`Hello Elite Wear, I want to order: ${name}. Please share availability and details.`;window.open(`https://wa.me/923345566399?text=${encodeURIComponent(text)}`,'_blank')}));
document.getElementById('cartBtn')?.addEventListener('click',()=>{if(!cart.length){alert('Your cart is empty. Choose a product to order.');return}const text=`Hello Elite Wear, I want to order these products:\n- ${cart.join('\n- ')}\nPlease confirm availability and total price.`;window.open(`https://wa.me/923345566399?text=${encodeURIComponent(text)}`,'_blank')});
const modal=document.getElementById('searchModal'),input=document.getElementById('searchInput'),results=document.getElementById('searchResults');document.getElementById('searchBtn')?.addEventListener('click',()=>{modal.classList.add('open');input.focus()});document.querySelector('.close')?.addEventListener('click',()=>modal.classList.remove('open'));modal?.addEventListener('click',e=>{if(e.target===modal)modal.classList.remove('open')});input?.addEventListener('input',()=>{const q=input.value.toLowerCase().trim();results.innerHTML='';if(!q)return;products.filter(p=>p.innerText.toLowerCase().includes(q)).forEach(p=>{const item=document.createElement('div');item.className='search-item';item.textContent=p.querySelector('h3').textContent;results.appendChild(item)});if(!results.children.length)results.innerHTML='<div class="search-item">No matching products found.</div>'});
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.style.opacity='1';e.target.style.transform='translateY(0)'}}),{threshold:.08});document.querySelectorAll('.category,.product,.review').forEach(el=>{el.style.opacity='0';el.style.transform='translateY(15px)';el.style.transition='opacity .6s ease, transform .6s ease';observer.observe(el)});
